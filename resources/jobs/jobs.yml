resources:
  jobs:
    Job_BUNDLE:
      name: "JOB_BUNLDE"
      tasks:
        - task_key: ingestion
          notebook_task:
            notebook_path: ../../src/notebook/notebook.ipynb
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZED