# This is a Databricks asset bundle definition for dabtutoriel.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabtutoriel
  uuid: bffb3edd-e478-4b1d-8fb6-03cd63ed530b



include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-aa10a61f-0222.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-aa10a61f-0222.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/azedine.zaaraoui@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/azedine.zaaraoui@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: azedine.zaaraoui@gmail.com
        level: CAN_MANAGE
